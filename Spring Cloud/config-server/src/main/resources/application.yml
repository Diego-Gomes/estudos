server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Diego-Gomes/microservice-repo
          search-paths: fornecedor, loja, transportador
         